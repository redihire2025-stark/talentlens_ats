# routes

Placeholder for URL-based routing if/when TalentLens needs deep-linkable
screens (e.g. shareable report links in a later version). V1's primary flow
(`src/App.tsx`) is a single-page view-state machine — see
`docs/architecture/overview.md` for why no router is introduced yet.
