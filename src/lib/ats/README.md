# lib/ats

The ATS Compatibility engine: `skillMatcher`, `keywordMatcher`,
`titleMatcher`, `experienceMatcher`, `educationMatcher`,
`responsibilityMatcher`, `formattingAnalyzer`, `sectionAnalyzer`,
`scoreCalculator` — one file per concern. See
`docs/scoring/scoring-methodology.md` for what each module evaluates.
Implemented in TASK-008 and TASK-010.
