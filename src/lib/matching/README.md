# lib/matching

Resume ↔ Job Description matching engine. Layered matching (exact →
normalized → synonym → fuzzy → optional semantic) plus JD match score
calculation. See `docs/scoring/matching-rules.md`. Implemented in TASK-009
and TASK-010.
