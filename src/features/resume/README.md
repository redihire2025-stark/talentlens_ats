# features/resume

Upload and processing UI for the candidate's resume (today's
`ResumeUpload.tsx` / `ProcessingScreen.tsx` prototypes live in
`src/components` and will move here as they're wired to real state in
TASK-013).
