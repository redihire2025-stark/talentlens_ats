# features/reports

Recommendation cards and export flow (today's `Recommendations.tsx` /
`ExportModal.tsx` prototypes). Wired to the recommendation engine and
export pipeline in TASK-011 and TASK-018.
