# features/resume-editor

Structured resume editor with live score recalculation, and the
before/after comparison view (today's `ResumeEditor.tsx` /
`BeforeAfter.tsx` prototypes). Wired to the editor + versions stores in
TASK-016 and TASK-017.
